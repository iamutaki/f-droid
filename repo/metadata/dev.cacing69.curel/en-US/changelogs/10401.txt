- remove BitbucketClient - API limitations (10-item pagination, multipart push, no clean deletion). kept GitHub, GitLab, Gitea only
- remove all Bitbucket references from git_providers_page.dart (dropdown, _getClient, placeholder hints)
- centralize GitClient factory into GitClient.create() on abstract class - eliminates duplicated _getClient in git_sync_service.dart and git_providers_page.dart. adding new providers now only requires editing git_client.dart
- fix blank screen after workspace change - pop settings page before triggering App rebuild via _workspaceKey++
- fix workspace buttons overflow - replace Row with Wrap for responsive layout on smaller screens
- fix conflict resolution loop - add force param to pull() to skip conflict detection guard when user already chose resolution strategy
- add error feedback in conflict resolution - both pull and push branches now show toast on failure instead of silently swallowing errors
- fix GitConnectDialog accepting empty URL - early return if url or branch is empty before popping result
- add double-tap guard on sync button - early return if _isSyncing is true before triggering sync
- fix copyWith nullable field clearing - use sentinel value pattern so disconnect can clear git fields with single copyWith call instead of fragile manual constructor
- fix secure keyboard appearing in curl editor - remove enableSuggestions: false which triggers Android IME_FLAG_NO_PERSONALIZED_LEARNING
- optimize sync() triple provider resolution - extract _pullImpl/_pushImpl that accept pre-resolved context, reducing secure storage reads from 3x to 1x per sync
- update memory files (git_sync_maturity.md, product_roadmap.md, MEMORY.md) with finalized Phase 2 state
- update README.md with current features, add "Why Curel?" section (free, open source, local-first, git-native, privacy)
- add persistent sync log in terminal area — new `log` field on ResponseState, green-themed "sync log" display in ResponseViewer, sync results written to log instead of toast-only, cleared on new request execution
