- add collection export feature (curel → Postman, Insomnia, Hoppscotch, Curel Native)
- add ExportedProject, ExportedEnv, ExportedRequest data classes to collection_adapter.dart
- add export() abstract method to CollectionAdapter interface
- implement PostmanAdapter.export() with curl parsing, folder tree, var conversion <<>> → {{}}
- implement InsomniaAdapter.export() with resources array, request_group folders, env resources
- implement HoppscotchAdapter.export() with items tree, environment mapping
- implement CurelNativeAdapter.export() delegating to existing curel JSON format
- add findById() to AdapterRegistry for adapter lookup by id
- add exportProjectAs(projectId, adapterId) to WorkspaceService
- add _buildExportedEnvs() and _buildExportedRequests() helpers in WorkspaceService
- update project_list_page.dart: export button now shows format picker popup menu
- add _doExport(), _iconFor(), _extFor() helpers in project list page
- add ConflictDialog widget with side-by-side diff viewer for git sync conflicts
- ConflictDialog shows local vs remote content with per-file resolution (L/R toggle)
- add DiffType enum and _DiffLine class for line-level diff rendering
- add pullWithResolution() to GitSyncService for per-file conflict resolution during pull
- add pushWithResolution() to GitSyncService for per-file conflict resolution during push
- replace simple 3-button conflict dialog in env_bar.dart with ConflictDialog flow
- remove old _showConflictDialog() method from env_bar.dart
- add listBranches() and createBranch() to GitClient abstract interface
- implement listBranches() and createBranch() in GitHubClient (REST: /branches, /git/refs)
- implement listBranches() and createBranch() in GitLabClient (REST: /repository/branches)
- implement listBranches() and createBranch() in GiteaClient (REST: /branches)
- add listBranches() and createBranch() wrappers to GitSyncService
- create BranchPickerDialog widget with branch list and new branch creation
- create _CreateBranchDialog for naming new branches
- add tappable branch chip in env_bar.dart next to sync icon
- add _openBranchPicker() method with switch-branch flow (update project + pull)
